import React from 'react';

const Overview = (props) =>{
    const {first, last, title, address, phone, description} = props;

    return (
        <div className='personal-info'></div>
    )
}

export default Overview;